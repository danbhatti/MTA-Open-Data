# MTA-Open-Data

This Python code will take a station input and color all other stations relative to the input, based on how many trains you must ride between them. I made a much more flexible version of this using JavaScript in another repo (so that you can click directly on the map and the see the pixels update).
